{
  "name": "task-manager-app",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "deploy": "npm run build --workspace client && npx firebase-tools deploy --only hosting"
  },
  "workspaces": [
    "client"
  ]
}